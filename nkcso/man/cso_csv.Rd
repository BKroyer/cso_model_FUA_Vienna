% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nkcso.R
\name{cso_csv}
\alias{cso_csv}
\title{Calculate CSO values from a CSV file}
\usage{
cso_csv(
  filename = "",
  url = "",
  population = 0,
  area = 0,
  delim = ";",
  col_timestep = 1,
  col_precipitation = 2
)
}
\arguments{
\item{filename}{character. Path to the CSV file containing the precipitation data.
If empty and \code{url} is provided, the file will be downloaded first.}

\item{url}{character. Optional URL to download the CSV file from the internet.}

\item{population}{numeric. Population of the catchment area. Must be > 0.}

\item{area}{numeric. Catchment area in km?. Must be > 0.}

\item{delim}{character. Column delimiter in the CSV file. Default: \code{";"}.}

\item{col_timestep}{numeric. Column number containing the timesteps (e.g. date or datetime). Default: 1.}

\item{col_precipitation}{numeric. Column number containing precipitation values \link{mm}. Default: 2.}
}
\value{
A data frame with approximately 50 columns containing CSO results,
including overflow volumes, durations, dry-weather flow shares, and event-based metrics.
}
\description{
This function reads precipitation data from a CSV file (or optionally from a URL),
combines it with population and area data, and calculates combined sewer overflow (CSO)
volumes, durations, and related hydrological parameters using the CSO model described
in Quaranta et al. (2022) and adapted in the master's thesis.
}
\details{
This function is part of the R-based implementation of the CSO hydrological model:
\itemize{
\item Quaranta, E., Fuchs, S., Liefting, H.J., Schellart, A., & Pistocchi, A. (2022).
A hydrological model to estimate pollution from combined sewer overflows at the regional scale: Application to Europe.
\emph{Journal of Hydrology: Regional Studies, 41}, 101080. https://doi.org/10.1016/j.ejrh.2022.101080
\item Kleemeyer, N.(2025). Hydrological Modelling of Combined Sewer Overflows (CSOs): An R-Based Implementation of Quaranta et al.'s Approach.
Master's Thesis, University of Vienna.
}

The function automatically handles downloading data if a URL is provided, reads the CSV file,
and passes the precipitation data to \code{cso_data()} for CSO calculations.
}
\examples{
# Calculate CSO results from a local CSV file
data <- cso_csv("vienna.csv", population = 2000000, area = 146)

# Download a CSV file from the internet and run the calculation
data <- cso_csv(url = "https://example.com/precipitation.csv",
                population = 1500000, area = 500)

}
\seealso{
\code{\link[=cso_data]{cso_data()}} for the core calculation function and \code{\link[=cso_defaults]{cso_defaults()}} for default parameter settings.
}
